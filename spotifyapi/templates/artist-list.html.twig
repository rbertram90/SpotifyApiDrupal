<h3>Some artists</h3>

{% for artist in artists %}
  <div class="artist">
    <h4>{{ artist.name }}</h4>
    <img src="{{ artist.images[2].url }}" alt="">
    <a href="{{ artist.external_urls.spotify }}" target="_blank">View artist on Spotify</a> |
    <a href="/view-artist/{{ artist.id }}">View more information on {{ artist.name }}</a>
  </div>
{% endfor %}
